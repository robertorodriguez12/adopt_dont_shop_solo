<!DOCTYPE html>
<html lang="en">
<head>
<title>CSS Template</title>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
* {
  box-sizing: border-box;
}

body {
  font-family: Arial, Helvetica, sans-serif;
}

/* Style the header */
header {
  background-color: #000000;
  padding: 30px;
  text-align: center;
  font-size: 35px;
  color: white;
}

/* Create two columns/boxes that floats next to each other */
nav {
  float: left;
  width: 30%;
  height: 300px; /* only for demonstration, should be removed */
  background: #CED500;
  padding: 20px;
}

/* Style the list inside the menu */
nav ul {
  list-style-type: none;
  padding: 0;
}

article {
  float: left;
  padding: 0px;
  width: 70%;
  background-color: grey;
  height: 300px; /* only for demonstration, should be removed */
}

/* Clear floats after the columns */
section:after {
  content: "";
  display: table;
  clear: both;
}

/* Style the footer */
footer {
  background-color: #000000;
  padding: 40px;
  text-align: center;
  color: white;
}

/* Responsive layout - makes the two columns/boxes stack on top of each other instead of next to each other, on small screens */
@media (max-width: 600px) {
  nav, article {
    width: 100%;
    height: auto;
  }
}
</style>
</head>
<body>

<header>
  <h2>Say Hello To:<%= @pet.name %></h2>
</header>
<% if @pet.image? %>
<%= binding.pry %>
 <%= image_tag @pet.image %>
<% end %>
<section>
  <nav>
    <ul>
      <li><%= button_to 'Pets', "/pets", method: :get %></li>
    </ul>
  </nav>

  <article>
    <label for="age"><%= @pet.name %>Age</label><br>
    <p><%= @pet.approximate_age %></p>
    <label for="sex"><%= @pet.name %>Gender</label><br>
    <p><%= @pet.sex %></p>
  </article>
</section>
<footer>
  <p>Thank you for Visiting</p>
</footer>

</body>
</html>
