<h1> Available Pets </h1>
<% @pets.each do |pet| %>
  <h2><%= pet.name %></h2>
  <%= image_tag(pet.image) %>
  <p>Approximate Age:<%= pet.approximate_age %></p>
  <p>Gender:<%= pet.sex %> </p>
  <p>Current Shelter:<%= pet.name_of_shelter_where_currently_located%> </p>

<% end %>
